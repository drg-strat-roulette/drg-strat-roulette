export interface Achievement {
	id: number;
	name: string;
	description: string;
	// subCheckboxes?: { id: number; name: string }[];
	// subAchievements?: number[];
	// quantity?: number;
	// minimumHazard?: number;
	// category?: string; // Category or subAchievements or both?
}
